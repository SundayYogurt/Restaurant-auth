FROM node:22-alpine

WORKDIR /app 
#สร้าง folder app ใน container

COPY package*.json ./
#copy package#.json * คืออะไรก็ได้ที่ตามหลังด้วย json

RUN npm install

COPY . .
# . ที่ 1 คือ client . ที่สอง คือ /app

EXPOSE  3000
# เปิด port ให้ container เชื่อมต่อกับ port ภายนอก

CMD [ "npm", "start"]
#คำสั่งไว้ รัน dev ใช้ cmd
